# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  assign_code_experts:
    if: 
      - true
    run:
      - action: add-reviewers@v1
        args:
          reviewers: {{ repo | codeExperts(lt=100) }}
